#include<stdio.h>
/**
 * main -A program that prints alphabets in lower case
 *
 * Return: 0
 */

int main(void)
{
	char i = 97;

	for (; i <= 122; i++)
	{
	putchar(i);
	}
	putchar(10);
	return (0);
}

#include<stdio.h>
/**
 * main -A program that prints the alphabet in lowercase
 *
 * Return: 0
 */

int main(void)
{
	int i = 61;

	for (; i <= 122;)
		(i++);
	{
	putchar(i);
	}
	putchar(10);
	return (0);
}

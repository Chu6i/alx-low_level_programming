#include "main.h"

/**
* print_alphabet - Prints the all the alphabets in lower case
*/

void print_alphabet(void)
{
	int alpha = 97;

	for (; alpha <= 122; alpha++)
	_putchar(alpha);
	_putchar(10);
}
